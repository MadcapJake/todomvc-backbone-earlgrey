require-macros: "../macros.eg" -> model
require: backbone
provide: Todo

model Todo:
   defaults = {
      title = ""
      completed = false
   }

   toggle = ->
      this.save with { completed = not this.get(.completed) }
