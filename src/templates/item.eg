module.exports = (completed, title) ->
   %
      div.view %
         input.toggle %
            type = .checkbox
         label % title
         button.destroy %
      input.edit %
         value = title
